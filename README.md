# RED Winter Character Sheet Roller (GitHub Pages)

This repo hosts `base_rolls.html` via GitHub Pages.

- Local preview: open `index.html` or `base_rolls.html` in a browser
- GitHub Pages: enable Pages for the default branch (root)
